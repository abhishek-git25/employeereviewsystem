<div class="container">
    <h2 class="mb-3">Assign Reviewers for Employee's Performance Review</h2>
    <form class="mt-4" id="assignReviewForm" action="/assign-employee-reviewer" method="POST">
        <div class="form-group mb-3">
            <label for="reviewers">Select Reviewers:</label>
            <select name="reviewedBy" id="reviewers" class="form-select">
                <option label="Select Reviewers"></option>
                <% employees.forEach((employee)=> { %>
                    <option value="<%= employee._id  %>">
                        <%= employee.username %>
                    </option>
                    <% }) %>
            </select>
        </div>
        <div class="form-group mb-3">
            <label for="employeeId">Select Employee:</label>
            <select name="reviewedFor" id="reviewedFor" class="form-select" required>
                <option label="Reviewee"></option>
                <% employees.forEach((employee)=> { %>
                    <option value="<%= employee._id  %>">
                        <%= employee.username %>
                    </option>
                    <% }) %>
            </select>
        </div>


</div>
<div class="d-flex mt-5"><button type="submit" class="btn btn-primary mx-auto">Assign Reviewers</button></div>

</form>
</div>
<script>


    var reviewers = document.getElementById('reviewers');
    var reviewedFor = document.getElementById('reviewedFor')



    // $(document).ready(function () {

    //     $('#assignReviewForm').submit(function (e) {
    //         e.preventDefault(); // Prevent the default form submission

    //         const formData = {
    //             reviewedFor: reviewedFor.value,
    //             reviewedBy: reviewers.value
    //         };

    //         $.ajax({
    //             type: 'POST',
    //             url: '/assign-employee-reviewer', // Replace with your server endpoint
    //             data: formData,
    //             success: function (data) {
    //                 // Handle the success response
    //                 new Noty({
    //                     theme: 'relax',
    //                     text: data.message,
    //                     type: 'success',
    //                     layout: 'topRight',
    //                     timeout: 1500
    //                 }).show();
    //                 console.log(data, 'Success');
    //             },
    //             error: function (error) {
    //                 // Handle the error response
    //                 new Noty({
    //                     theme: 'relax',
    //                     text: error.responseJSON.error,
    //                     type: 'error',
    //                     layout: 'topRight',
    //                     timeout: 1500
    //                 }).show();
    //             },
    //         });
    //     });
    // });



</script>